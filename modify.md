# 知识库分块逻辑修改汇报草稿

## 1. 本轮修改范围

和负责人沟通后，本轮先只做“知识库”的分块和向量化逻辑调整，暂时不处理故障库。

需要注意的是：虽然当前只做知识库，但字段设计不能把逻辑写死成“知识库/故障库”二选一。后续系统可能继续增加其他库，所以 `document_library_type` 仍然保留，用来表示文档所属库类型。

本轮重点是：

1. 调整知识库文档表结构。
2. 新增知识库章节/分块表。
3. 知识库按章节生成 chunk。
4. 每个章节暂时对应一个向量 chunk。
5. 保留 `main_chunk`，但改成适合知识库的摘要字段。
6. 扩充 Milvus metadata，新增 `tag`。

## 2. 当前问题

目前系统里的文档结构更偏向故障文档：

```text
Document
├── title
├── problem_intro
├── causes
├── evaluation
├── inspection
├── solutions
└── key_points
```

这些字段适合故障库，因为故障文档天然包含问题简介、原因、评估、检查、解决方案等内容。

但知识库文档不一定有“故障原因”“检查”“解决方案”这些概念。知识库可能是设备说明、操作流程、维护规范、参数解释、注意事项、培训材料等。如果继续使用故障文档字段，会导致：

- 字段语义不准确。
- AI 总结时会被迫填入不合适的故障字段。
- 后续分块和 `main_chunk` 会带上“故障原因”等不适合知识库的内容。
- 前端以后改知识库录入页面时，后端旧结构会形成阻碍。

因此，本轮需要把知识库从“故障文档结构”里解耦出来。

## 3. 知识库表结构需要调整

知识库理论上不应该继续依赖 `problem_intro`、`causes`、`evaluation`、`inspection`、`solutions` 这些故障字段。

建议知识库主表保留更通用的文档级信息，例如：

| 字段                   | 含义             | 说明                                                                                                                                                |
| ---------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                 | 文档 id          | 知识库文档主键。                                                                                                                                    |
| `title`              | 文档标题         | 知识库文章、资料或文件标题。                                                                                                                        |
| `contributor_id`     | 贡献者 id        | 上传或创建该知识文档的用户。                                                                                                                        |
| `first_edit_date`    | 首次编辑时间     | 保留当前文档管理逻辑。                                                                                                                              |
| `image_urls`         | 文档图片汇总     | 可选字段，保存整篇知识库文档涉及的图片列表，方便列表页或详情页快速展示。图片和正文位置关系应以章节表中的 `image_urls`、`image_positions` 为准。 |
| `summary`            | 文档摘要         | 知识库文档的整体摘要，可由 AI 生成或人工填写。                                                                                                      |
| `content`            | 文档全文或主内容 | 如果后续主要依赖章节表，也可以作为冗余全文或展示用字段。                                                                                            |
| `origin_file_name`   | 原始文件名       | 导入文件时记录来源。                                                                                                                                |
| `origin_file_dir`    | 原始文件路径     | 导入文件时记录存储路径。                                                                                                                            |
| `tag`                | 标签             | JSON 数组，保留现有标签体系。                                                                                                                       |
| `is_vectorized`      | 是否已向量化     | 标记是否已写入 Milvus。                                                                                                                             |
| `vector_update_time` | 向量更新时间     | 记录最近一次向量化时间。                                                                                                                            |
| `is_deleted`         | 是否删除         | 软删除标记。                                                                                                                                        |

具体字段还需要结合当前前端和接口最终确认，但原则是：知识库表不要继续以故障原因、检查、解决方案作为核心结构。

## 4. 新增知识库章节/分块表

建议新增一张章节/分块表，用来保存知识库文档下的结构化章节。

表名可以暂定为：

```text
document_sections
```

或者如果本轮只服务知识库，也可以命名为：

```text
knowledge_document_sections
```

为了后续扩展到其他库，更倾向于使用通用表名 `document_sections`，并通过 `document_library_type` 标识所属库。

### 字段说明

| 字段                      | 含义         | 说明                                                                                                                                                               |
| ------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`                    | 章节块主键   | 每个章节块唯一 id。                                                                                                                                                |
| `document_id`           | 所属文档 id  | 当前章节属于哪篇文档。当前本轮主要对应知识库文档 id。                                                                                                              |
| `document_library_type` | 文档库类型   | 当前可以填知识库对应的库类型，但代码不要写死只支持知识库或故障库。后续如果增加其他库，可以继续复用。                                                               |
| `section_index`         | 章节顺序     | 当前章节在文档中的顺序。用于展示、拼接上下文和向量结果排序。                                                                                                       |
| `section_title`         | 章节标题     | 原始文档中的标题，或者前端录入的章节标题。                                                                                                                         |
| `section_type`          | 章节类型     | 可选字段，用于标识章节来源或类型，例如 `heading`、`paragraph`、`procedure`、`notice`、`knowledge_section` 等。不要使用故障库专属类型作为知识库默认结构。 |
| `plain_text`            | 章节纯文本   | 当前章节的主要文本内容。每个章节本轮会直接生成一个 chunk。                                                                                                         |
| `image_urls`            | 章节图片     | 当前章节关联的图片。建议保存为 JSON 数组，便于记录图片顺序和位置。                                                                                                 |
| `image_positions`       | 图片位置     | 建议新增。用于记录图片在章节中的位置，例如图片出现在第几段之后、字符位置附近，或前后文。                                                                           |
| `char_start`            | 原文开始位置 | 章节在原始文档文本中的起始字符位置。没有原文位置时可以为空。                                                                                                       |
| `char_end`              | 原文结束位置 | 章节在原始文档文本中的结束字符位置。没有原文位置时可以为空。                                                                                                       |
| `metadata`              | 扩展信息     | JSON 字段，保存解析来源、页码、标题层级、文件路径、图片附近文本等扩展信息。                                                                                        |
| `created_time`          | 创建时间     | 章节块创建时间。                                                                                                                                                   |
| `updated_time`          | 更新时间     | 章节块更新时间。                                                                                                                                                   |

### 本轮不纳入表格

之前草稿里提到的 `table_content` 本轮先不做。

原因是负责人已确认：表格不纳入本轮修改范围。后续如果要支持表格，可以再新增 `table_content` 或单独设计表格结构。

## 5. 不做手动文档字段映射

之前考虑过把手动文档的现有字段映射成章节，例如：

```text
title + problem_intro -> 问题简介章节
causes -> 原因章节
solutions -> 解决方案章节
```

负责人确认后，本轮不做这个映射。

原因是前端后续会修改知识库录入方式。如果现在为了兼容旧页面强行做字段映射，虽然短期可以跑通，但会把旧的故障字段逻辑继续带进知识库，反而影响后续前端和数据结构调整。

因此，本轮知识库章节应该直接来自：

- 修改后的知识库前端录入结构。
- 导入文档解析出的真实章节。
- 或后端新增的知识库章节接口。

不要再把知识库文档强行转换成 `problem_intro`、`causes`、`solutions` 这种故障字段结构。

## 6. 每个章节暂时对应一个 chunk

之前参考别人代码时，提到过对章节内部继续做语义切分：

- 短 chunk 合并。
- 大 chunk 拆分。
- 句子级 embedding。
- 语义相似度合并。
- 图片和文本 unit 共享 `chunk_id`。

负责人确认后，本轮暂时不做这些复杂逻辑。

当前目标是：

```text
一个章节块 -> 一个 Milvus chunk
```

也就是说，每条 `document_sections` 记录直接生成一个向量 chunk。

这样做的好处：

- 改动范围更小。
- 数据链路更清楚。
- 便于先验证章节表和知识库向量化流程。
- 后续如果发现章节过长，再在章节内部增加二级语义分块。

需要注意：虽然本轮不做章节内部拆分，但 metadata 里可以先保留 `subchunk_index`、`semantic_method` 等字段，方便后续兼容升级。

## 7. 图片处理方式

负责人确认：图片继续采用“字段文本/章节文本 + 图片描述”合成 chunk 的方式。

本轮可以理解为：

```text
章节文本 + 图片描述 -> 章节 chunk
```

如果一个章节内有图片，则：

1. 读取章节文本 `plain_text`。
2. 找到章节关联图片。
3. 调用图片描述模型生成图片描述。
4. 将图片描述追加到章节 chunk 文本中。
5. 在章节表或 metadata 中记录图片位置。

需要重点注意：不仅要知道这个 chunk 有哪些图片，还要知道图片在章节中的大致位置。

建议记录两类信息：

- `image_urls`：有哪些图片。
- `image_positions` 或 `metadata.image_positions`：图片位置，例如第几段后、字符位置、附近文本、原文页码等。

这样后续前端展示或拼接上下文时，才能把图片放回正确位置。

## 8. 保留 main_chunk，但改为知识库字段

当前 `main_chunk` 逻辑偏故障库，内容类似：

```text
问题简介：...
核心成因：...
关键特征：...
```

这不适合知识库，因为知识库文档不一定有“问题简介”和“核心成因”。

本轮建议保留 `main_chunk` 这个“文档级召回入口”，但生成内容改成知识库通用字段，例如：

```text
文档标题：...
文档摘要：...
核心主题：...
适用范围：...
关键内容：...
相关标签：...
```

也可以简化为：

```text
标题：...
摘要：...
核心主题：...
关键知识点：...
标签：...
```

这样 `main_chunk` 仍然可以帮助系统先召回相关知识文档，但不会再出现故障原因、检查、解决方案等故障库字段。

## 9. 扩充 Milvus metadata

Milvus 中存的是最终检索 chunk。本轮每个章节对应一个 chunk，所以 metadata 需要能回溯到：

- 文档。
- 库类型。
- 章节。
- 图片位置。
- 标签。

### 字段说明

| 字段                | 含义           | 说明                                                                                                    |
| ------------------- | -------------- | ------------------------------------------------------------------------------------------------------- |
| `source_doc_id`   | 原始文档 id    | 当前 chunk 来自哪篇知识库文档。                                                                         |
| `library_type`    | 文档库类型     | 当前本轮主要是知识库，但字段不要写死为某两个固定库。后续可支持更多库类型。                              |
| `section_id`      | 章节块 id      | 对应章节表 `document_sections.id`。用于从 chunk 回查完整章节。                                        |
| `section_title`   | 章节标题       | 方便检索结果展示，例如显示“命中章节：维护注意事项”。                                                  |
| `section_index`   | 章节顺序       | 用于按原文顺序排序和拼接上下文。                                                                        |
| `subchunk_index`  | 章节内子块顺序 | 本轮每个章节只有一个 chunk，所以可以固定为 `0`。后续章节内部二次切分时再递增。                        |
| `unit_type`       | 检索单元类型   | 本轮可以主要是 `text`，因为图片描述会合入文本 chunk。后续如果图片单独入库，可以扩展为 `image`。     |
| `content_type`    | 内容类型       | 本轮可以是 `text_with_image_description` 或 `section_text`，表示章节文本可能包含图片描述。          |
| `chunk_id`        | chunk 标识     | 当前 chunk 的唯一标识。因为本轮一个章节一个 chunk，可以和 `section_id` 建立稳定关系，也可以独立生成。 |
| `chunk_size`      | chunk 长度     | 当前 chunk 文本长度，包括章节文本和图片描述。用于调试是否过长。                                         |
| `semantic_method` | 分块算法版本   | 本轮可以标记为 `knowledge_section_v1`，表示按知识库章节直接分块。                                     |
| `tag`             | 文档标签       | 建议新增。保存知识库文档标签，后续可能用于按标签过滤、检索加权或结果展示。                              |
| `image_urls`      | 图片列表       | 建议新增或放入 metadata。记录当前 chunk 关联图片。                                                      |
| `image_positions` | 图片位置       | 建议新增或放入 metadata。记录图片在章节中的位置或附近文本。                                             |

### tag 字段说明

负责人提到后续可能需要根据 `tag` 搜索，所以 metadata 里建议加上 `tag`。

用途包括：

- 检索前按标签过滤。
- 检索后按标签展示或分组。
- 对某些标签的知识文档做召回加权。
- 用户选择标签后，只在相关知识范围内搜索。

当前文档表里已有 `tag` 字段，向量化时可以同步写入 Milvus metadata。

## 10. 本轮明确不做的内容

为了控制范围，本轮暂时不做：

1. 故障库分块逻辑改造。
2. 手动文档旧字段到章节的映射。
3. 章节内部短 chunk 合并。
4. 章节内部长 chunk 拆分。
5. 句子级语义相似度分块。
6. 表格解析和表格 chunk。
7. 图片单独作为 image unit 入 Milvus。

这些可以作为后续优化方向。

## 11. 本轮目标流程

本轮目标流程可以简化为：

```text
知识库文档
    -> 知识库章节表 document_sections
        -> 每个章节生成一个 chunk
            -> chunk 文本 = 章节文本 + 图片描述
                -> 写入 Milvus
```

检索时：

```text
用户问题
    -> Milvus 检索 chunk
        -> 通过 metadata 知道 source_doc_id、section_id、tag、image_urls、image_positions
            -> 回查知识库文档和章节
                -> 组织上下文给大模型回答
```

## 12. 需要再次确认的问题

正式改代码前，还需要确认：

1. 知识库主表最终字段是否采用通用结构，例如 `title`、`summary`、`content`、`tag`。
2. 章节表使用通用表名 `document_sections`，还是知识库专用表名 `knowledge_document_sections`。
3. `document_library_type` 当前知识库的取值是什么，是否沿用现有 `knowledge`。
4. 图片位置字段单独建列 `image_positions`，还是放在 `metadata` JSON 中。
5. 章节图片描述是每次向量化实时生成，还是生成后缓存到章节表 metadata 中。
6. `main_chunk` 的知识库摘要字段最终定为哪些，例如摘要、核心主题、关键知识点、适用范围、标签。
7. Milvus metadata 中 `tag` 使用数组、字符串，还是同时保存标签 id 和标签名。

## 13. 一句话总结

本轮不是做完整的语义分块系统，而是先把知识库从故障文档字段里拆出来，建立“知识库文档 -> 章节 -> chunk”的基础链路。

当前阶段：

```text
一个知识库章节 = 一个 Milvus chunk
```

chunk 内容包含章节文本和图片描述，metadata 记录文档、章节、标签和图片位置信息。后续如果章节过长，再在这个基础上增加语义拆分、合并和表格处理。
